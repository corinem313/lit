# Under construction page

Author this folder in a code editor. It is not a Gutenberg page.

## Styles (SCSS)

1. `npm install`
2. Edit `style.scss`
3. `npm run watch` while developing, or `npm run build` once

`index.html` loads `style.css` (the compiled file). Do not link `style.scss` in the HTML.
